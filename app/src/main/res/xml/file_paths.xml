<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Temp files for camera captures live in cache/camera and are shared with the
         camera app via FileProvider. -->
    <cache-path name="camera_captures" path="camera/" />
</paths>
